#!/usr/bin/env bash
# Legacy wrapper: use subscriber_mac for IP+MAC binding (ARP and/or FW_BRIDGE_MACFIX).
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/subscriber_mac" "$@"
